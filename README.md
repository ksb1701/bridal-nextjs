This is the conversion of a single bridal webpage based on WordPress + PHP to Next.js + Express.js/Node.js (if required).

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.
